project Blink is

   for Languages use ("Ada", "ASM_CPP");
   for Source_Dirs use ("src");
   for Object_Dir use "obj";
   for Main use ("blink.adb");
   for Target use "arm-eabi";
   for Runtime ("ada") use "zfp-cortex-m3";

   package Linker is
      for Switches ("ada") use ("-T", "C://Users//ADMIN//gpr//blink///src/link.ld");
   end Linker;

   package Ide is
      for Vcs_Kind use "git";
      for Vcs_Repository_Root use ".";
   end Ide;

end Blink;

